import { describe } from "bun:test";

describe("JournallingStore", () => {
	describe("start()", () => {
		// TODO(vxern): Add tests.
	});

	describe("stop()", () => {
		// TODO(vxern): Add tests.
	});

	describe("tryLog()", () => {
		// TODO(vxern): Add tests.
	});
});
