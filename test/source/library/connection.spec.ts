import { describe } from "bun:test";

describe("DiscordConnection", () => {
	describe("open()", () => {
		// TODO(vxern): Add tests.
	});

	describe("close()", () => {
		// TODO(vxern): Add tests.
	});
});
