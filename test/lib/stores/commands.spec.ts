describe("CommandStore", () => {
	describe("create()", () => {
		it("creates an object.", () => {
			// TODO(vxern): Test.
		});
	});

	describe("build()", () => {
		// TODO(vxern): Tests.
	});

	describe("buildCommand()", () => {
		// TODO(vxern): Tests.
	});

	describe("buildOption()", () => {
		// TODO(vxern): Tests.
	});

	describe("getHandler()", () => {
		// TODO(vxern): Tests.
	});

	describe("isShowable()", () => {
		// TODO(vxern): Tests.
	});

	describe("hasRateLimit()", () => {
		// TODO(vxern): Tests.
	});

	describe("getEnabledCommands()", () => {
		// TODO(vxern): Tests.
	});

	describe("getRateLimit()", () => {
		// TODO(vxern): Tests.
	});
});

describe("isAutocomplete()", () => {
	it("returns true when the interaction is autocomplete.", () => {
		// TODO(vxern): Implement.
	});

	it("returns false when the interaction is not autocomplete.", () => {
		// TODO(vxern): Implement.
	});
});

describe("isSubcommandGroup()", () => {
	it("returns true when the option is a subcommand group.", () => {
		// TODO(vxern): Implement.
	});

	it("returns false when the option is not a subcommand group.", () => {
		// TODO(vxern): Implement.
	});
});

describe("isSubcommand()", () => {
	it("returns true when the option is a subcommand.", () => {
		// TODO(vxern): Implement.
	});

	it("returns false when the option is not a subcommand.", () => {
		// TODO(vxern): Implement.
	});
});
