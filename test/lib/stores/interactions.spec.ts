describe("InteractionStore", () => {
	describe("constructor()", () => {
		it("creates an object.", () => {
			// TODO(vxern): Test.
		});
	});

	describe("spoofInteraction()", () => {
		// TODO(vxern): Add tests.
	});

	describe("registerInteraction()", () => {
		// TODO(vxern): Add tests.
	});

	describe("unregisterInteraction()", () => {
		// TODO(vxern): Add tests.
	});

	describe("acknowledge()", () => {
		// TODO(vxern): Add tests.
	});

	describe("postponeReply()", () => {
		// TODO(vxern): Add tests.
	});

	describe("reply()", () => {
		// TODO(vxern): Add tests.
	});

	describe("editReply()", () => {
		// TODO(vxern): Add tests.
	});

	describe("deleteReply()", () => {
		// TODO(vxern): Add tests.
	});

	describe("respond()", () => {
		// TODO(vxern): Add tests.
	});

	describe("displayModal()", () => {
		// TODO(vxern): Add tests.
	});
});

describe("createModalComposer()", () => {
	// TODO(vxern): Add tests.
});

describe("parseComposerContent()", () => {
	// TODO(vxern): Add tests.
});

describe("parseTimeExpression()", () => {
	// TODO(vxern): Add tests.
});

describe("parseConciseTimeExpression()", () => {
	// TODO(vxern): Add tests.
});

describe("parseVerboseTimeExpressionPhrase()", () => {
	// TODO(vxern): Add tests.
});
