describe("Client", () => {
	describe("documents()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("localise()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("localiseUnsafe()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("pluralise()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("isShowable()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("registerInteraction()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("unregisterInteraction()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("acknowledge()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("postponeReply()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("reply()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("editReply()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("deleteReply()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("respond()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("displayModal()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("registerCollector()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("tryLog()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("lavalinkService()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("interactionRepetitionService()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("realtimeUpdateService()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("statusService()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("getAlertService()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("getDynamicVoiceChannelService()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("getEntryService()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("getMusicService()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("getRoleIndicatorService()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("getNoticeService()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("getPromptService()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("registerInteractionCollector()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("bot()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("entities()", () => {
		// TODO(vxern): Add tests. Check delegates or retrieves value.
	});

	describe("create()", () => {
		it("creates an object.", () => {
			// TODO(vxern): Add test.
		});
	});

	describe("start()", () => {
		// TODO(vxern): Add tests.
	});

	describe("stop()", () => {
		// TODO(vxern): Add tests.
	});

	describe("reloadGuild()", () => {
		// TODO(vxern): Add tests.
	});

	describe("handleInteraction()", () => {
		// TODO(vxern): Add tests.
	});

	describe("resolveIdentifierToMembers()", () => {
		// TODO(vxern): Add tests.
	});

	describe("resolveInteractionToMember()", () => {
		// TODO(vxern): Add tests.
	});

	describe("autocompleteMembers()", () => {
		// TODO(vxern): Add tests.
	});
});

describe("isValidSnowflake()", () => {
	// TODO(vxern): Test.
});

describe("extractIDFromIdentifier()", () => {
	// TODO(vxern): Test.
});

describe("isValidIdentifier()", () => {
	// TODO(vxern): Test.
});
