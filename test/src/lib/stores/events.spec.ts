import { describe } from "bun:test";

describe("EventStore", () => {
	describe("dispatchEvent()", () => {
		// TODO(vxern): Add tests.
	});

	describe("registerCollector()", () => {
		// TODO(vxern): Add tests.
	});
});
