import { describe } from "bun:test";

describe("isAutocomplete()", () => {
	// TODO(vxern): Test.
});

describe("isSubcommandGroup()", () => {
	// TODO(vxern): Test.
});

describe("isSubcommand()", () => {
	// TODO(vxern): Test.
});
